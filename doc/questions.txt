- "Assume no Ns" ?
    Exclude in both chromosomes

- Columns in VCF
    ID
        Different ID for each line
    FILTER 'PASS'
    INFO '.'
    FORMAT 'GT'?

- Probability
    random() < 0.0025 OK?
        Do it more deterministically (mutate every 400 + randint(0,100)

